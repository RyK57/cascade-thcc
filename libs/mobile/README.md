# Capacitor mobile scaffold

Add Capacitor when you need native iOS/Android shells:

```bash
pnpm add @capacitor/core @capacitor/cli @capacitor/ios @capacitor/android
pnpm exec cap init
```

Keep shared business logic in `libs/` and reuse from web + mobile.
